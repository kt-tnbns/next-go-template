package repository

// Package repository holds repository interfaces (contracts) for persistence.
// Implementations live in internal/infrastructure/persistence.
// Add interfaces here as the project grows, e.g. UserRepository, CarRepository.
// Placeholder file to keep the package and directory in the Clean Architecture structure.
var _ = struct{}{}
